# پیشرفت پروژه‌ی واژه‌بازی

این فایل منبعِ حقیقت برای کارهای باقی‌مانده است.

## نحوه‌ی کار

۱. کلود تسک بعدی را با معیارهای پذیرشش اعلام می‌کند.
۲. **پیاده‌سازی با توست.**
۳. وقتی تمام شد اعلام می‌کنی؛ کلود بازبینی می‌کند (کد + اجرای تست/بیلد).
۴. اگر معیارها پاس شد، تسک تیک می‌خورد و می‌رویم سراغ بعدی.

کلود تسک‌ها را پیاده‌سازی نمی‌کند — فقط تعریف و بازبینی.

---

## وضعیت فعلی

- تاریخ شروعِ این فاز: **۲۰ سپتامبر ۲۰۲۶** (۲۹ شهریور ۱۴۰۵)
- پایه‌ی ساخته‌شده: React 18 + Vite + JavaScript + CSS Modules، بازی حدس‌واژه کامل و کارکن
- وابستگی‌های اجرایی: `react`، `react-dom`، `react-router-dom`

---

## تصمیم‌های قطعی‌شده

| موضوع | تصمیم |
|---|---|
| نسخه‌ی TypeScript | **۵.۹** — نه ۷. دلیلش در یادداشت تسک ۱ پایین آمده. |
| مبنای روز | **نیمه‌شب `Asia/Tehran`** — کلمه‌ی همه در یک لحظه یکی است، مستقل از ساعت دستگاه |
| شروع تقویم بازی | **۱ جولای ۲۰۲۶** = پازل شماره‌ی ۱ (پس امروز ۲۰ سپتامبر ۲۰۲۶ = پازل **۸۲**) |
| پازل‌های گذشته | **قابل بازی‌کردن** هستند، نه فقط قابل دیدن |
| مسیر APK | **Capacitor** (بدون نیاز به دامنه و هاست) |
| ذخیره‌سازی | فعلاً فقط localStorage، ولی با ساختاری که مهاجرت به دیتابیس بدون درد باشد |
| بازی امروز | **همیشه رایگان** — سکه هیچ‌وقت جلوی پازل امروز را نمی‌گیرد |

---

## باگ شناخته‌شده (در تسک ۲ حل می‌شود)

`getDailyPuzzle()` از نیمه‌شبِ **محلیِ دستگاه** حساب می‌کند. نتیجه‌اش این است که
در یک لحظه‌ی واحد (۲۰ سپتامبر ۲۰۲۶، ۲۰:۳۰ UTC):

```
Asia/Tehran          → پازل ۹۹۵ (رهبری)
Europe/Berlin        → پازل ۹۹۴ (رقابت)
America/Los_Angeles  → پازل ۹۹۴ (رقابت)
Pacific/Kiritimati   → پازل ۹۹۵ (رهبری)
```

یعنی خواسته‌ی «کلمه‌ی همه یکی باشد» برآورده نمی‌شود، و جلو بردن ساعت گوشی
کلمه‌ی فردا را لو می‌دهد.

---

## تسک‌ها

| # | تسک | وضعیت |
|---|---|---|
| ۱ | مهاجرت به TypeScript | ✅ |
| ۲ | مبنای زمانی و کلمه‌ی روز | ⬜ |
| ۳ | بزرگ‌کردن فهرست کلمه‌ها | ⬜ |
| ۴ | بازطراحی لایه‌ی ذخیره‌سازی | ⬜ |
| ۵ | تاریخچه‌ی هفت‌روزه | ⬜ |
| ۶ | نمای تقویم | ⬜ |
| ۷ | بازی پازل‌های گذشته | ⬜ |
| ۸ | مفهوم سکه (غیرفعال) | ⬜ |
| ۹ | PWA | ⬜ |
| ۱۰ | خروجی APK با Capacitor | ⬜ |

---

### تسک ۱ — مهاجرت به TypeScript ✅

**انجام شد در ۲۴ سپتامبر ۲۰۲۶.**

**معیار پذیرش:**

- [x] `typescript`، `@types/react@^18`، `@types/react-dom@^18` به‌عنوان devDependency نصب شده‌اند
- [x] `tsconfig.json` ساخته شد (یک فایل، بدون `references` — دلیلش پایین)
- [x] `strict: true` و `noUncheckedIndexedAccess: true` روشن است
- [x] همه‌ی `.jsx` به `.tsx` و همه‌ی `.js` داخل `src/` به `.ts` تبدیل شده‌اند
- [x] `npm run typecheck` بدون خطا پاس می‌شود
- [x] هیچ `any`، `@ts-ignore` یا `@ts-expect-error`ای در `src/` نیست
- [x] props همه‌ی کامپوننت‌ها با `interface` تایپ شده‌اند (نه `React.FC`)
- [x] `src/types.ts` با تایپ‌های دامنه ساخته شد
- [x] ESLint با `typescript-eslint` کار می‌کند — ۳۲ فایل واقعاً لینت می‌شوند
- [x] `npm run build` بدون warning پاس می‌شود
- [x] هیچ تغییر رفتاری در بازی ایجاد نشد (۴۸ تست رفتاری + ۲۳ تست منطقی پاس شد)

**تصمیم‌ها و انحراف‌ها:**

۱. **TypeScript روی ۵.۹ پین شد، نه ۷.** `typescript-eslint` صراحتاً TS 7 را رد
   می‌کند (`typescript-eslint does not support TS 7.0`) و بدون آن ESLint اصلاً
   نمی‌تواند فایل `.tsx` را پارس کند. پیگیری پشتیبانی:
   [typescript-eslint#10940](https://github.com/typescript-eslint/typescript-eslint/issues/10940).
   هر وقت پشتیبانی آمد، ارتقا فقط عوض‌کردن نسخه است.

۲. **`tsconfig.node.json` حذف شد.** الگوی `references` نیاز به `composite: true`
   دارد و با `noEmit` جمع نمی‌شود. یک `tsconfig.json` که هم `src` و هم
   `vite.config.ts` را پوشش می‌دهد ساده‌تر و بدون هزینه است.

۳. **`global.d.ts` با `src/vite-env.d.ts` جایگزین شد** که `vite/client` را
   ارجاع می‌دهد؛ این هم CSS module، هم CSS ساده و هم تصویرها را پوشش می‌دهد.

۴. **`src/data/types.ts` حذف و محتوایش منتقل شد.** تایپ کارت‌های صفحه‌ی خانه
   مخصوص همان صفحه است و داخل `Home.tsx` ماند؛ تایپ‌های مشترک در `src/types.ts`.

۵. **`Settings` و `Stats` هم نام تایپ‌اند هم نام کامپوننت مودال.** در سه فایلی
   که هر دو لازم‌اند، تایپ با نام دیگری وارد می‌شود (`Settings as UserSettings`).

### تسک ۲ — مبنای زمانی و کلمه‌ی روز

**هدف:** روزِ بازی به نیمه‌شب تهران قفل شود و تقویم بازی از ۱ جولای ۲۰۲۶ شروع شود.

**معیار پذیرش:**

- [ ] `EPOCH` برابر **۱ جولای ۲۰۲۶** است و آن روز پازل شماره‌ی **۱** است
- [ ] امروز (۲۰ سپتامبر ۲۰۲۶) پازل شماره‌ی **۸۲** است
- [ ] روزِ بازی از `Asia/Tehran` حساب می‌شود، نه از منطقه‌ی زمانی دستگاه
- [ ] تست: در چهار منطقه‌ی زمانی مختلف (`TZ=...`) در یک لحظه‌ی واحد، شماره‌ی پازل **یکسان** است
- [ ] تست: پازلِ یک تاریخ مشخص همیشه همان کلمه را می‌دهد (پایداری)
- [ ] تابعی برای تبدیل «شماره‌ی پازل ↔ تاریخ» وجود دارد (تسک‌های تاریخچه و تقویم لازمش دارند)
- [ ] درخواست پازلی با شماره‌ی کمتر از ۱ یا بزرگ‌تر از امروز، خطای مشخص می‌دهد
- [ ] اسکریپت `npm run word` اضافه شده که کلمه‌ی امروز را نشان می‌دهد، و با
      `npm run word -- 2026-07-15` کلمه‌ی هر تاریخی را
- [ ] بازیِ ذخیره‌شده‌ی قبلی (با شماره‌ی پازل قدیمی) باعث خطا نمی‌شود

**نکته:** برای محاسبه‌ی «روز در تهران» از `Intl.DateTimeFormat('en-CA', { timeZone: 'Asia/Tehran' })`
استفاده کن که خروجی `YYYY-MM-DD` می‌دهد. آفستِ ثابت (`+3:30`) هاردکد نکن.

---

### تسک ۳ — بزرگ‌کردن فهرست کلمه‌ها

**هدف:** یک سال بازیِ بدون تکرار، و رد نشدنِ حدس‌های معقول.

**معیار پذیرش:**

- [ ] `answers` حداقل **۳۶۵** کلمه دارد (الان ۱۱۱)
- [ ] `words` حداقل **۱۰۰۰** کلمه دارد (الان ۲۲۲)
- [ ] اسکریپت `npm run words:check` اضافه شده که این‌ها را تضمین می‌کند:
  - [ ] طول هر کلمه دقیقاً ۵ حرف است (با `[...w].length`، نه `w.length`)
  - [ ] هیچ کلمه‌ی تکراری‌ای نیست
  - [ ] `answers` زیرمجموعه‌ی `words` است
  - [ ] هیچ حرف عربی (`ك`، `ي`، `ة`) یا اعراب یا نیم‌فاصله‌ای در فهرست نیست
  - [ ] هیچ دو کلمه‌ای بعد از نرمال‌سازی یکی نمی‌شوند
- [ ] `npm run words:check` در CI/قبل از بیلد قابل اجراست
- [ ] ترتیب کلمه‌های موجود در `answers` **دست‌نخورده** مانده (کلمه‌های جدید فقط به انتها اضافه شده‌اند)

**هشدار:** اسکریپت فقط طول و یکتایی و املای فنی را چک می‌کند. **درستی معنایی و
املاییِ کلمه‌ها را باید انسان تأیید کند.** اگر فهرست آماده‌ای از یک پیکره‌ی فارسی
داری، خیلی مطمئن‌تر از دست‌چین کردن است.

---

### تسک ۴ — بازطراحی لایه‌ی ذخیره‌سازی

**هدف:** ساختاری که فردا بشود بدون بازنویسی منطق بازی، به دیتابیس واقعی مهاجرت کرد.

**معیار پذیرش:**

- [ ] یک ماژول repository با امضای **آسنکرون** ساخته شده (`Promise` برمی‌گرداند)،
      حتی وقتی پشتش localStorage است
- [ ] هیچ جای دیگری از پروژه مستقیماً `localStorage` صدا نمی‌زند
- [ ] هر رکورد `schemaVersion` و `updatedAt` دارد
- [ ] بازی‌ها به‌ازای **هر پازل** ذخیره می‌شوند (`vajebazi:game:82`)، نه یک کلید واحد
- [ ] یک تابع migration وجود دارد که داده‌ی فرمت قدیمی (`vajebazi:game`) را به فرمت
      جدید تبدیل می‌کند و بعد پاکش می‌کند
- [ ] در schema جای خالی برای **کیف پول سکه** رزرو شده (تسک ۸)، تا بعداً migration دوباره لازم نشود
- [ ] یک `interface` برای repository تعریف شده که پیاده‌سازی localStorage آن را برآورده می‌کند
      (تا بعداً پیاده‌سازی HTTP جایش بنشیند)
- [ ] تست: نوشتن، خواندن، migration از فرمت قدیم، و رفتار درست وقتی localStorage در دسترس نیست
- [ ] بازیِ نیمه‌کاره و آمارِ کاربر با به‌روزرسانی از دست نمی‌رود

---

### تسک ۵ — تاریخچه‌ی هفت‌روزه

**هدف:** زیر برد بازی بشود اسکرول کرد و هفت روز گذشته را دید.

**معیار پذیرش:**

- [ ] زیر بازی یک بخش تاریخچه هست که ۷ روز گذشته را نشان می‌دهد
- [ ] هر روز: تاریخ شمسی، شماره‌ی پازل، و وضعیت (`برده` / `باخته` / `نیمه‌کاره` / `بازی‌نکرده`)
- [ ] برای روزهای برده‌شده، تعداد حدس‌ها هم نمایش داده می‌شود
- [ ] **کلمه‌ی روزهایی که بازی نشده‌اند لو نمی‌رود**
- [ ] روزهای قبل از پازل ۱ (قبل از ۱ جولای ۲۰۲۶) نمایش داده نمی‌شوند
- [ ] همه‌ی اعداد با ارقام فارسی
- [ ] در عرض ۳۲۰px بدون اسکرول افقیِ صفحه جا می‌شود
- [ ] با `prefers-reduced-motion` سازگار است

---

### تسک ۶ — نمای تقویم

**هدف:** نمای ماهانه که وضعیت هر روز را یک‌جا نشان بدهد.

**معیار پذیرش:**

- [ ] تقویم **شمسی** است (نه میلادی)، با نام ماه فارسی
- [ ] هر روز با رنگ/نشانه وضعیتش را نشان می‌دهد: برده / باخته / نیمه‌کاره / بازی‌نکرده
- [ ] رفتن به ماه قبل و بعد ممکن است
- [ ] رفتن به قبل از ۱ جولای ۲۰۲۶ و بعد از امروز ممکن نیست
- [ ] وضعیت‌ها در حالت رنگ‌کوری هم قابل تشخیص‌اند (فقط با رنگ تفکیک نشده باشند)
- [ ] راهنمای رنگ‌ها (legend) دارد
- [ ] در ۳۲۰px جا می‌شود

---

### تسک ۷ — بازی پازل‌های گذشته

**هدف:** از تاریخچه یا تقویم بشود روی یک روز گذشته زد و همان‌جا بازی کرد.

**معیار پذیرش:**

- [ ] مسیری مثل `/wordle/:puzzle` وجود دارد
- [ ] بازیِ آرشیوی state و ذخیره‌سازی **مستقل** از بازی امروز دارد
- [ ] بازی نیمه‌کاره‌ی آرشیوی هم با رفرش برمی‌گردد
- [ ] درخواست پازل نامعتبر (قبل از ۱، بعد از امروز) به صفحه‌ی مناسب هدایت می‌شود
- [ ] در هدر مشخص است که داری پازل کدام روز را بازی می‌کنی
- [ ] متن اشتراک‌گذاری شماره‌ی پازل درست را می‌زند
- [ ] **تصمیم باز:** بازی آرشیوی روی آمار و استریک اثر بگذارد یا نه؟
      پیشنهاد کلود: در `played` / `wins` / توزیع حساب شود، ولی **روی استریک اثر نگذارد**
      (استریک فقط مال روزهای پیاپیِ واقعی است). قبل از شروع این تسک تصمیم می‌گیریم.
- [ ] نقطه‌ای که بعداً سکه باید جلویش را بگیرد، مشخص و ایزوله است (تسک ۸)

---

### تسک ۸ — مفهوم سکه (فعلاً غیرفعال)

**هدف:** زیرساخت سکه ساخته شود ولی پشت یک feature flag خاموش بماند.

**معیار پذیرش:**

- [ ] کیف پول در همان لایه‌ی repository تسک ۴ ذخیره می‌شود (`balance` + تاریخچه‌ی تراکنش‌ها)
- [ ] هر تراکنش ثبت می‌شود: مقدار، علت (`archive-play` / `second-chance` / `purchase`)، زمان
- [ ] یک feature flag واحد کل قابلیت را خاموش/روشن می‌کند؛ **پیش‌فرض خاموش**
- [ ] وقتی خاموش است: هیچ نشانه‌ای از سکه در UI دیده نمی‌شود و بازی آرشیوی آزاد است
- [ ] وقتی روشن است: بازی پازل گذشته سکه کم می‌کند، و بعد از باخت گزینه‌ی «شانس دوباره» ظاهر می‌شود
- [ ] **بازی امروز در هر حالتی رایگان است** — این را تست کن
- [ ] موجودی هیچ‌وقت منفی نمی‌شود؛ کم‌کردن سکه اتمیک است
- [ ] خرید واقعی (پرداخت) در این تسک **نیست** — فقط مدل داده و گیت

---

### تسک ۹ — PWA

**هدف:** نصب‌شدن روی گوشی و کار کردن کامل آفلاین.

**معیار پذیرش:**

- [ ] `manifest.webmanifest` با نام فارسی، `dir: "rtl"`، `lang: "fa"`، `display: standalone`
- [ ] آیکون‌های ۱۹۲ و ۵۱۲ پیکسل، به‌علاوه‌ی نسخه‌ی `maskable`
- [ ] service worker که کل app shell را کش می‌کند
- [ ] بازی با قطع کامل اینترنت کار می‌کند (فونت Vazirmatn هم باید آفلاین بیاید — یعنی لوکال شود)
- [ ] استراتژی به‌روزرسانی مشخص است و نسخه‌ی قدیمی گیر نمی‌کند
- [ ] در Lighthouse بخش PWA سبز است
- [ ] روی اندروید واقعی «افزودن به صفحه‌ی اصلی» درست کار می‌کند
- [ ] ترجیحاً بدون افزودن وابستگی (service worker دستی) — بازی نیاز کشِ پیچیده‌ای ندارد

---

### تسک ۱۰ — خروجی APK با Capacitor

**هدف:** یک APK قابل نصب.

**معیار پذیرش:**

- [ ] Capacitor نصب و پیکربندی شده، پلتفرم اندروید اضافه شده
- [ ] `npm run build` + sync، APK می‌سازد
- [ ] APK روی دستگاه/شبیه‌ساز واقعی نصب و اجرا می‌شود
- [ ] بدون اینترنت کار می‌کند
- [ ] localStorage بین اجراهای اپ باقی می‌ماند
- [ ] دکمه‌ی back اندروید رفتار درستی دارد (مودال را می‌بندد، از بازی به خانه برمی‌گردد، و ته صفحه از اپ خارج می‌شود)
- [ ] آیکون و نام فارسیِ اپ درست است
- [ ] جهت RTL و فونت روی WebView اندروید درست است
- [ ] مراحل ساخت در README ثبت شده

**پیش‌نیاز بیرونی:** JDK و Android Studio روی سیستم.

---

## تصمیم‌های باز

| موضوع | کِی تصمیم می‌گیریم |
|---|---|
| اثر بازی آرشیوی روی آمار و استریک | قبل از تسک ۷ |
| منبع فهرست کلمه‌ها (دست‌چین یا پیکره‌ی آماده) | قبل از تسک ۳ |
| قیمت‌گذاری و روش خرید سکه | بعد از تسک ۸ |
| دامنه و میزبانی برای نسخه‌ی وب | قبل از تسک ۹ |

---

## گزارش بازبینی‌ها

| تسک | تاریخ | نتیجه | یادداشت |
|---|---|---|---|
| ۱ | ۲۳ سپتامبر ۲۰۲۶ | ⏳ ناتمام | خطای نحوی در `Keyboard.tsx:7` جلوی بیلد را گرفته و ۱۱۹ خطای تایپ را پنهان کرده. ESLint هنوز هیچ فایل TSای را نمی‌بیند. وابستگی‌های TS در `dependencies` به‌جای `devDependencies`. اسکریپت `typecheck` اضافه نشده. |
| ۱ | ۲۴ سپتامبر ۲۰۲۶ | ✅ تمام | تایپ‌ها توسط کلود کامل شد. `typecheck`، `lint` و `build` پاس می‌شوند؛ ۳۲ فایل واقعاً لینت می‌شوند؛ ۴۸ تست رفتاری بدون تغییر پاس شد. TypeScript روی ۵.۹ پین شد چون `typescript-eslint` هنوز TS 7 را پشتیبانی نمی‌کند. |
